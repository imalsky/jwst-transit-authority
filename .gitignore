# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.DS_Store

# Generated caches (model spectra + pandeia results) -- regenerated per run
output/*
!output/.gitkeep

# Editor / OS noise
.vscode/
.idea/

# Packaging build artifacts
*.egg-info/
build/
dist/

# Large synphot reference data (fetch from ssb.stsci.edu/trds; worker preflight guides you)
data/cdbs/calspec/

# Large pandeia reference data (~4.3 GB per release; see README "Backend
# configuration"). Globbed, not per-release: these were pinned to the 2026.2
# names, so adding the 2026.7 triple made 4.3 GB show up as untracked and one
# `git add -A` away from the history.
data/pandeia_data-*/
data/pandeia_psfs-*/
# Internal working documents. Reference content and dev logs live in the
# gitignored root notes.md; README.md carries current usage only.
CLAUDE.md
/notes.md

# Machine-local absolute-path symlink into a sibling PICASO checkout. Consumers
# obtain the PHOENIX grid via `jwst-tool fetch`; `jwst-tool data` reports status.
data/cdbs/grid/phoenix

